{
  "_comment": "The task envelope from article 1, fig. 04. Values in ALL_CAPS are placeholders substituted at submission time -- see submit.py in a real deployment. This repo ships the template, not a live submission script, since running it requires a real Batch account, pool, and identity.",
  "id": "RUN_ID",
  "commandLine": "python -m ingest.main --run-id RUN_ID --window-start WINDOW_START --window-end WINDOW_END --table-uri TABLE_URI --source synthetic-data/orders_sample.json",
  "containerSettings": {
    "imageName": "IMAGE_DIGEST",
    "containerRunOptions": "--rm"
  },
  "environmentSettings": [
    { "name": "RUN_ID", "value": "RUN_ID" },
    { "name": "AZURE_CLIENT_ID", "value": "MANAGED_IDENTITY_CLIENT_ID" }
  ],
  "constraints": {
    "maxTaskRetryCount": 2,
    "retentionTime": "PT1H"
  },
  "userIdentity": {
    "autoUser": {
      "scope": "task",
      "elevationLevel": "nonadmin"
    }
  }
}
